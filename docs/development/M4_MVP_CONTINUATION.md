# 本机 MVP 续开发

2026-09-20。接续 `MVP_DELIVERY_PLAN.md` 与 `M4_SCOPED_FINALE.md`。当前工作工程为 `AI-Script-Game`；资料库中的 `ai-jubensha-fusion` 是较早检查点，不在该副本重复开发。

## 本轮任务约定

- 目标：解决已冻结失败样本中的题目指代与事件混用，推进可供集中试玩的本机候选。
- 范围：先将已审的两条题面澄清加入新的私有候选并验证真人/AI一致；核实当前供应商的推理上限契约，再决定是否实现独立的新私卷策略。供应商契约未证实时不付费探参。
- 保持：原题选项、计分、真相、所有旧绑定/请求/回执/费用；公开终局策略不借本轮私卷变更自动升级。公网部署、TTS及新供应商不在本轮范围。
- 完成判据：新题面只在依据已授权时出现；旧局投影不变；受影响的模型输入、事件恢复与费用预占通过检查；新模型候选必须有独立来源复核，才能启用。结构通过不能替代语义通过。
- 验证：`backend/.venv/bin/python backend/scripts/test_fusion_security.py`、`backend/.venv/bin/python scripts/dev_preflight.py`、`backend/.venv/bin/python -m unittest discover -s scripts -p 'test_dev_preflight.py'`、`git diff --check`；新增行为定向测试、私有来源/旧局对照与独立审查。若修改前端，再运行该目录要求的测试、类型、lint和构建。
- 费用与假设：沿用原 M4 200 次/¥10，上一封存检查点175次/¥3.73549424；实际请求前重新核算共享账本。原批次关闭，未知请求不重发。真实麦克风按用户既有决定稍后验，不将其登记为已通过。

## 当前调查

现有私卷1.5已经保留完整选项、来源片段和题面范围。新增长提示会挤压获准材料：上一 H 样本98,299字节，上限98,304。采用新推理模式前必须分别核实供应商支持、硬上限、正文与推理计量、结构化非流式响应；不能简单全局开启 thinking。

Y 的两条澄清草案已在上一轮只读报告完成来源对照；它们可区分事前/事后请求，不能补足现有复合选项的缺失证据。未知仍合法。商业题面、来源和详细审查继续保存于仓库外。

## 已完成的候选和验证

新私有候选追加 Y 的两条已审澄清，H 原样，沿用已有私卷1.5及新局范围快照。此候选显式仅允许离线构建；`live=True` 在读取配置、状态或预占前拒绝，不能被误当成新付费批次。上一候选源码摘要实际核验；已执行的历史离线费用夹具保留，另附说明，未重置真实账本。

T/Y 两种真人选角均完成模拟整局：共58次调查、82次刷新、8份模拟答卷、两次结算；每次检查的40份旧局投影保持。真人Y与AI Y的27道题面、选项和上限逐项一致，两条澄清在依据缺失时均恢复原题。原用户数据库与来源文件摘要保持。此处模拟答卷使用UNKNOWN，不作为真实内容正确性的证据。

后端安全套件3524项、预检及14项自测通过；独立复核无剩余阻塞。前端未改，未重复计算历史前端测试。本轮新增真实模型/语音调用0、费用0；共享账本只读复核仍175次/¥3.73549424，原200次/¥10不变。现有3017/API8017均返回200，未替换服务。

## 供应商能力核验与下一步

[DTMaaS官方公开模型详情](https://maas.antdigital.com/api/v1/model-service/public/get/modelService-1776177344042001233)声明现用模型支持思考，并列出thinking_budget默认4000。但公开元数据没有定义该预算的硬停止语义、正文/推理的总量关系或reasoning用量子计数。[快速接入文档](https://maas.antdigital.com/dt-maas-docs/quick-access)展示普通非流式调用，尚不足以证实思考、非流式JSON与费用硬限的组合契约。不能把平台字段存在当成这些条件已经通过测试，也不据某个UI enable标记断言参数必定无效。

本轮未开启推理、切换模型或追加付费探参。已向用户提出路线选择：允许评估其他模型/现有直连接口，仍守原总预算；或继续限定当前蚂蚁模型。未收到答复前不视为批准切换。下一步继续M4 A，先确定可验证的模型契约，再做新版本私卷的固定失败场景实验；公开终局的既有归属问题、最终五角色真实矩阵、桌面麦克风与集中试玩仍未通过。

本轮状态是**题面候选离线验证完成，未启用，完整MVP未验收**。私有阶段目录为资料库下 `m4-mvp-continuation-20260920`，含两个整局报告、题面一致性/数据保持、独立review和官方接口契约核验；商业内容不进入Git。建议后续开发使用高思考强度，仅为建议设置。
