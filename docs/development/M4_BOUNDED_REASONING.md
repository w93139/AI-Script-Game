# 终局有限推理候选

2026-09-20，接续本机MVP工作包A。当前蚂蚁模型的终局内容仍未验收；本轮准备可审阅的百炼直连候选，不替换运行服务，不调用付费API，不将路线选择的未答复视为批准。

## 任务与验收约定

新私卷1.6/绑定1.8仅允许现有白名单中的百炼 `qwen3.7-flash-2026-07-15` 直连接口。基础角色对白、公开终局及旧私卷保持禁用思考；新私卷仅在SEAL_FINALE开启推理，调查投票仍用原禁用参数。题面、授权资料、完整选项、评分和原回执不改。

- 推理预算固定1024，生成总限为原答卷上限加1024。默认总限5120，费用预占5136，覆盖官方最多10 token误差。4096是可见答卷的接纳上限，不能声称服务端独立硬限正文生成到4096。
- 新策略、供应商契约、计量含义及保守费率写入不可变绑定和模型元数据。旧绑定无新字段，恢复按旧策略。冻结参数或来源被篡改时不得发送。
- 纯文本completion包含reasoning，只计费一次。新局部适配器处理百炼的inclusive text计数；不修改旧全局normalizer。推理分量缺失不能当零；不合法答卷不能发布，可信费用仍记账，未知使用量保留全部预占。
- 只解析最终JSON正文，内部推理不进入玩家视图、游戏事件或后续角色上下文。仍一次调用、不自动重试、不跨模型回退。
- 新策略最高输入预占102400，按32K—256K档保守估算：输入和缓存输入均至少¥0.6/M，输出至少¥2.4/M。低于此费率的新绑定在创建前拒绝，旧费率和旧局不回写。
- 验收覆盖适配器实际wire、完整来源、旧版快照、分量边界、五种真人选角的模拟结算、持久预占/结果/恢复、重复请求和失败记账；通过必需离线检查及独立审查后作为源码候选保存。真实模型质量仍需后续授权路线下的固定样本验收。

新功能无默认开关或环境变量自动启用。原M4总次数/费用不变；真实验证前仍需适配私有共享账本记录器、检查凭据和新批次，不能把现有蚂蚁记录器直接用于百炼费用计量。

## 官方依据与实施判断

[OpenAI兼容接口](https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions)定义max_completion_tokens为推理与回答总量（最多10 token误差），并说明text_tokens包含reasoning_tokens。[深度思考说明](https://help.aliyun.com/zh/model-studio/deep-thinking)定义thinking_budget达到上限后转入回答，并支持商业思考模型的非流式调用。[结构化输出](https://help.aliyun.com/zh/model-studio/qwen-structured-output)列出Qwen3.7-Flash支持JSON Schema。[模型快照价格](https://help.aliyun.com/zh/model-studio/qwen3-7-flash)给出32K—256K档费率。

上述文档支持实现此候选；思考、非流式与严格JSON Schema的组合是从官方能力声明得出的实施判断，尚未经过本账户真实调用验证。旧工程存在百炼配置，当前工程仍使用蚂蚁；本轮没有复制、展示或修改密钥。

## 离线验证与当前限制

本轮后端全量3659项通过（140.89秒）；开发预检退出0、预检自测14项及diff检查通过。新增135项合成测试覆盖计量、适配器与持久化；独立复审发现的两项问题已修复：服务端接受结果及事件回放再次检查限额，非空推理正文但计数为0时拒绝答卷。失败仍保存可信用量和费用。修复后针对性41项通过，复审无剩余Blocker或Important。

五种真人选角各完成本人和四AI模拟封卷、结算及冷恢复；重复同请求不再次调用，未知用量保留全额预占。此矩阵使用合成资料与模拟SDK，不作为真实模型内容验收。

另将既有J/H/Y/S四份真实授权上下文仅离线装配：wire分别80144、83314、81721、81145字节，均在98304字节上限内，完整source_context与原冻结文件保持。输入预占84240—87410、输出预占5136；没有发送请求。该检查沿用9月19日冻结题面，未把9月20日Y澄清替换进历史输入。

私有证据保存在仓库外 `m4-bounded-reasoning-20260920`，包括官方契约报告、容量记录及独立复核。没有新增真实调用或费用；累计仍175次/¥3.73549424（原200次/¥10），剩25次/¥6.26450576。未修改凭据、运行配置或3017/API8017服务。前端未改。

本轮候选不代表终局语义已修复。下一步先落实模型路线，并完成对应共享账本与隔离运行配置；有了可审阅批次后，才在原总预算内验证固定失败样本，再决定是否进入五角色真实矩阵。桌面真实麦克风与集中用户试玩仍未验收，完整MVP继续处于M4。建议开发使用高思考强度，原因是涉及费用、旧存档兼容和来源推断；这是建议，未改变会话设置。
