# M4 蚂蚁平台与资料定位核查

日期：2026-09-16。仅完成密钥接收、只读模型目录查询和资料完整性检查，未启动真实商业本、改写存档或接入新模型到游戏。

后续决定：用户已明确无法补49号扫描，并授权创作不在玩家卡面显示编辑标记的合理线索。采用稿已完成，见[49号线索采用记录](M4_CLUE49_ADOPTION.md)。下方“原件仍缺”保留为档案事实，不再作为继续开发时要求用户补件的条件。

## API 配置

- 用户实际使用蚂蚁数科 DTMaaS，不是火山方舟或百炼直连。
- 根目录私有 `.env` 新增 `ANT_MAAS_API_KEY` 接收位，既有配置保留；权限 0600，Git 忽略，未跟踪。已在本机文本编辑器打开供用户粘贴。
- 检测到用户保存的值非空；未显示、记录或复制密钥到报告。
- 按[官方快速接入](https://maas.antdigital.com/dt-maas-docs/quick-access)核对统一端点 `https://maas-api.antdigital.com/v1`。
- 使用已保存密钥只读请求 `/v1/models`：HTTP 200，本次响应解析出20个模型条目，其中包括 `qwen3.8-flash`、`deepseek-flash`、`glm-5.3-flash`。这是返回条目数量，不宣称账号全部权限或每个模型生成均已验证。
- 官网[模型市场](https://maas.antdigital.com/models)公共列表本次有103个条目；公共在架目录与账号可调用范围不能混为一谈。
- 现有游戏供应商注册表仍仅支持火山方舟和百炼。新变量目前只保存密钥，游戏尚不读取它；未把蚂蚁Key放到原供应商字段，也未绕过白名单。
- 付费与语音开关保持关闭；模型生成调用0次。未发送剧本正文给蚂蚁。

后续优先把千问 Flash 作为适配测试候选，实际选择需验证结构化输出、角色保密/引用、延迟与费用。平台的路由和重试行为需按[选择供应商说明](https://maas.antdigital.com/dt-maas-docs/choose-provider)核对，不能把第三方OpenAI兼容接口直接视为原供应商契约。

## 资料实际位置

新建 `<资料暂存目录>/待接入资料` 本轮检查只有 `.DS_Store`。在桌面旧项目找到实际资料：

`<旧项目目录>/private-data`

无需用户重新提供整套资料。此前“当前仓库未找到真实资料”的检查结论，仅适用于当时检查范围；现在已定位旧项目，尚未迁移接回。

## 独立只读校验结果

| 项目 | 实际检查 |
| --- | --- |
| 原始来源清单 | 362/362 文件存在且 SHA256 匹配，包含重复副本，去重181份 |
| 五角色原册 | 唐、护士、杰、海、玉各12张去重原图在场 |
| 组织册/主持来源 | 12张去重原图在场，现有清单未发现新增缺项 |
| 修复运行包来源 | 358/358 文件存在且 SHA256 匹配 |
| 登记展示图片 | 55/55 文件存在且 SHA256 匹配 |
| M4 候选包 | sources/visuals 与修复包相同 |
| 五角色专用议题/引导目录 | 10/10 JSON 在场 |
| 旧存档与启动材料 | live-state/play.sqlite、server.py、launch-config.json 等存在；未读取身份密钥、未启动或改写 |

这些结果证明登记文件仍在且与清单一致，不证明出版原件本身从未缺页，也不等于新版页面已正确显示全部资料。

## 线索卡是否补全

1. 历史漏显示的24张图片曾补齐授权；目前登记的55张展示图实际全部找回。**55是展示条目数，不是原版1–55号卡齐全的证明。**
2. **原版49号线索卡正面与背面仍缺失。** 地下室现有43–48及50，缺项清单仍保留49。
3. `reconstructed-clue-49.md/json` 存在，是历史授权制作的重建候选；不等于找回原件，不在本轮自动替换来源。
4. 12号背面文件名为 `12阿贝.jpg`，不要因名称未含“卡背”误判缺失。

主要核查文件（均在上述私有资料根目录内）：

- `script-library/nie-dao-yi-yun/source-manifest.json`
- `script-library/nie-dao-yi-yun/missing-sources.json`
- `import-jobs/m3-full-play/20260907-strategy-candidate-2/m3-user-validation/clue-presentation-20260909/image-index.json`
- `import-jobs/m3-full-play/20260907-strategy-candidate-2/m3-user-validation/content-repair-20260908/draft/candidate.json`
- `import-jobs/m3-full-play/20260907-strategy-candidate-2/m3-user-validation/m4-role-adaptation-20260910/candidate.json`

## 下一步

- 沿用 M4：先映射旧资料路径，保留原件与存档；旧 source_root/ocr_text_root 仍指不存在的 `AI人生海海/OCR`，旧启动器也绑定旧代码目录，不能直接运行当作恢复成功。
- 恢复当前已存在的文字和55张图片到新版，核对五角色授权和刷新。原版49卡继续登记缺失；若用户有扫描件，再做来源补录。
- 增加蚂蚁渠道适配，先用合成资料验证结构、费用和错误恢复，再进行有源真实角色验收。交付可试玩版本、模型验证结果及剩余缺项表。
- 建议极高思考强度，理由是来源路径、秘密权限、供应商行为与费用账本需要一起核对。

本轮未移动原资料、恢复数据库、启动旧服务、提交或推送GitHub。配置变更只新增密钥接收位；安全核对仅输出是否配置、文件权限及Git忽略状态，不输出秘密值。
