# M4 角色回答失败恢复

日期：2026-09-17。状态：本机已更新，等待用户复测。范围：已失败问题的明确恢复与回答呈现；保留原局、原模型回执与费用，不更改已冻结模型、目录、剧本或校验规则。

## 原因与交付约定

用户这局先后请求两位角色回答。只读事件和模型回执核对显示，前一条成功，后一条因引用类别不属于输入目录被拒绝。后一条接口正常返回，但正文还包含时间、引文覆盖和含义偏差，拒绝并非单纯误杀，不能改引用后直接放行。

目标是让失败不再只显示泛泛红字：在问题旁显示角色、成功回答或失败说明，允许用户明确选择该题已冻结的资料简答，保存后可继续游戏。后续成功操作不能重复显示历史模型错误。生成质量尚未修复，本轮不重发旧请求、不自动采用简答，也不将失败改写为成功。

验收：原局副本执行简答、刷新、继续游戏；核对不调用模型、不改变原失败回执；公开与私聊权限保持；前端相关回归、类型/lint/构建、仓库离线必需检查和独立复审；原数据库七表与真实账本逐项对照。

## 改动

- 本轮问题和回答放到提问区后、自由发言前，最新问题在前，显示角色与公开/单独频道。
- 成功公开回答只从服务器已授权回应中按原问题、角色和阶段匹配，直接放在问题旁边；不将私聊记录混入公开回答。
- 失败问题显示持久原因和“使用资料简答，继续游戏”。仅服务器允许时出现按钮，点击后使用原 USE_FALLBACK，明确它是资料简答，不再次请求模型。未知请求仍只能核对原键。
- 后续简答、调查、推进、暂停电话成功，不再因历史 last_ai_status=INVALID 重复显示失败红字。非议题模型失败仍给出错误提示。

当前对话模型在首次请求时冻结。直接修改旧提示或切换模型会破坏原局回放或触发配置冲突，不能作为本次失败的静默修补。后续生成质量改进须采用可追溯的新题目录或显式新模型版本；保留旧问题、回复和费用。

## 验证与交付

| 检查 | 实际结果 |
| --- | --- |
| 前端自动回归 | 62通过，含失败恢复、身份、冻结请求、公开答案匹配和历史错误提示 |
| 类型与lint / 生产构建 | 全通过，构建 dt1V_zP0Gf7zGDdQ_PpBP |
| 后端离线必需套件 | 3066通过 |
| 开发预检 / 自测 | 退出0 / 14通过 |
| 独立只读复审 | 最终无未解决发现，相关40项独立测试通过 |
| 原局副本实际浏览器 | 失败问题显式简答成功，revision16→17；刷新后答案保持；继续至第二轮阅读revision19，无错误红字 |
| 桌面 / 窄屏 | 1470/390px无页面横向溢出；实体手机待用户复测 |
| 原数据库 / 真实账本 | 七表全部逐行摘要保持：40局、43开场、1397事件；65账本文件哈希保持 |

副本使用真实目录与存档副本，模型调用和出网均被禁止；本轮真实调用0、费用0。原局仍revision16、阿海OK/阿杰FAILED，未代玩家采用简答或推进。用户此前两次提问合计¥0.020796，均按原回执保留，不能把它们计为本轮新增或抹去失败费用。M4累计120次、保守¥1.89693424，原¥10/200次上限保持。

仅更新并重启本机前端，入口仍 http://127.0.0.1:3017/，后端8017未变。没有修改模型提示、目录、校验器、存档或API Key；未提交推送。README仍为项目介绍。私有核查与浏览器证据在仓库外 m4-answer-recovery-20260917，商业原文不进入公开测试或本文。

## 用户下一步与剩余工作

刷新原局，在交流区阿杰的问题旁点“使用资料简答，继续游戏”，读完后继续提问或进入下一阶段。此操作保存资料简答，不会自动推进阶段。

本轮修复的是失败恢复与展示，模型引用和事实准确性尚未修复。下一轮仍为M4，针对引用类别、时间和片段覆盖做版本化修复，交付新请求的逐条来源对照与受限真实回归；同时保留旧局投影。建议开发思考强度极高，因为旧请求、角色权限、内容依据与费用须一起核对。当前不需要用户补资料或重新配置Key。
