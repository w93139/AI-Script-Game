# M4 终局证据契约候选

2026-09-18 开始，2026-09-19 更新：继续工作包 A。上一候选公开来源核查 3/4、私卷 0/4 整卷通过；工程流程通过不能替代来源正确。当前运行版本保持，真实麦克风由用户稍后测试。

## 本轮范围与验收

新增公开终局 1.5、私卷 1.3 及独立新局绑定。公开句子附私有片段摘录和事件、时间、身份关系；私卷逐题区分直接支持、合理推断和未知，引用当时获准材料的片段。未知保留空选项，不拿否定选项代替。玩家台词、原题、计分及独立投票不变，证据记录只在服务端保存。

已有提示、请求、回执、费用、旧局及其冻结输入保持。新片段传输须逐字保留来源，并按实际传输计量；新输出上限只作用于新策略。先完成契约、篡改、旧版本恢复和隐私回归，再冻结受限真实批次并独立对照来源。结构检查只能证明引用存在和协议一致，不能声称自动证明语义。

私有固定回归表覆盖八类场景、28 个原文锚点及五个冻结上下文。独立勘误修正题面归属、无效负证据、伪装选项与投票目标口径；原报告不重写，汇总结果不变。商业原文、完整输出与私人数据不进入公开仓库。

当前状态：契约工程已验证，真实回归仍暴露来源问题，未启用。实际检查、费用和运行版本见下；完整 MVP 仍需五角色当前版本真实回归和集中用户验收。

## 已完成的工程验证

- 后端完整检查 3313 项、预检及 14 项自测通过；独立新契约检查 89 项通过。首轮新增测试误把布尔假值当作 null，已修正测试并重跑完整检查。前端没有修改，未把上一阶段的前端 114 项重复记为本轮结果。
- 多选题改为每个所选项分别记录直接支持或推断、实际来源及短依据；同题五条独立来源及混合确定程度已覆盖。空选择表示未知，不自动补选项，也不把未选择当成已证明为假。
- 公开摘录须逐字存在于所引片段；关系包含同一事件、时序、身份和明确持久排他事实。字段通过只证明结构一致，不能证明摘录足以支持台词。
- 旧 table 1.0—1.2 与公开 1.0—1.4 的冻结请求保持；旧公开输出仍为 192，新版为 1024。新私卷证据只进服务端事件，玩家接口不显示；缺失、替换、跨版本和决定投影不一致均拒绝。未知或待定请求不重发，禁用模型仍生效。
- 五种真人选角的隔离 HTTP 模拟全部完成：145 次调查、205 次刷新、20 条公开终局、20 份 AI 私卷与五次结算；每次核对 40 份旧局投影一致。它们是模拟结果，不是模型语义通过。

## 本轮真实回归

只在新副本中运行 T 真人的一整局。四条公开终局中 H、Y 被判 INVALID 并留空，J、S 为 OK；四份私卷结构均为 OK。29 次调查、41 次刷新与最终结算通过，失败没有卡死整局，也没有重试原请求。逐输出来源核查另外登记，不能用 6/8 结构有效替代语义结论。

新增 8 次调用，保守估算 ¥0.37723400；本批 8 次 / ¥1.50 上限已用完次数。M4 累计 163 次 / ¥2.87607024，原 200 次 / ¥10 上限保持，最多剩 37 次 / ¥7.12392976。此前 12 次批次原样封存。语音本轮新调用 0。

原七表（40 局、43 开场、1397 事件）、169 个既有账本文件及 13 个冻结输入保持；本批只新增 24 个账本文件。来源、原始模型输出、完整回放和预算证据保存在仓库外私有目录。

## 启用与整体进度

仍为 M4 / 总计划 A，候选尚未启用。运行入口继续为 `http://127.0.0.1:3017`（API 8017），沿用 topic v4、公开终局 1.2、私卷 1.1 和前端构建 `GOg_ZIZaWQrchzLZGl6h1`；已验证启动文件未被本轮修改。

M1—M3 的既有文字流程保留；M4 的工程、来源质量与真实模型验收分开。后续仍需修复本批实际问题、补当前版本五角色真实回归、完成桌面真实麦克风与集中整局用户验收。用户选择稍后测试麦克风，该项保持待验，不阻止继续独立开发；MVP 尚未通过总验收。
