# 人生海海 · 迭代方案

> 项目体检日期：2026-09-12
> 共 15 件事，按需求等级从高到低排序。一级不做完不要上线，二级三级可以慢慢来。
>
> **执行进度（2026-09-12）**
> 一级 5 项全部完成；二级完成 07、08、09、10，仅剩 06；三级 5 项未动。
> 本机运行环境（PostgreSQL 17 + Redis 7.4）已搭好，前后端可正常启动。
> 后端 3249 项 + 12 项数据库集成测试、前端 536 项全部通过。

---

## 本次跑通结果

| 检查项 | 结果 |
| --- | --- |
| 后端自动检查 | 3168 / 3170 通过 |
| 前端自动检查 | 523 / 523 通过 |
| 正式打包 | 28 页成功 |
| 本次花费 | ¥0.00，未调用任何付费模型 |

没通过的 2 项，是老代码里写错了预期值（它还以为注册功能开着，其实早关了），不影响使用。另有 12 项数据库检查因为这台电脑没装数据库，被项目自带的安全护栏主动挡下了，也不算问题。

**一句话结论：现在这个状态直接上线，第一天就会有人用别人的账号登录进来。把一级的五件事做完，其余的都可以从容来。**

---

## 一级 · 必做（5 项）— ✅ 已完成

> 判断标准：不做完就会造成真实损失——账号被盗、后台被进、被人刷钱。这五件没做完，不要对外开放。

### ✅ 01. 关掉假的短信验证码

现在验证码永远是 **123456**，而且网站会主动把这个码告诉任何来问的人。手机验证码又是**唯一**的登录方式（用户名密码注册已经关了）。等于门锁密码是 123456，门上还贴了张纸写着密码。

- **怎么做**：接一家真实的短信服务商。在接通之前，宁可先把手机登录整体关掉，也不要带着假验证码上线。
- **验收**：拿一个别人的手机号，配 `123456`，登不进去。

### ✅ 02. 让程序开机先自查，配置不对就拒绝启动

系统给每个用户发的"通行证"，是用一把签名钥匙盖章的。现在如果安装时忘了换这把钥匙，程序**不会报错**，而是默默用代码里写死的那个公开默认值——谁都能查到它。我实测过：用这个公开默认值，自己就能做一张管理员通行证进后台。

- **怎么做**：开机时先自查一遍——签名钥匙没换、验证码还是假的、文件存储还在用出厂默认密码，任何一条不合格就直接拒绝启动并说清楚原因。让"忘了改"变成不可能，而不是靠人记得。
- **验收**：故意把钥匙那一项删掉，程序应该起不来，并告诉你缺了什么。

### ✅ 03. 加上次数限制

整个系统**没有任何防刷机制**。别人可以一秒钟试一万次密码、要一万次验证码，没人拦。

- **怎么做**：登录、要验证码这些入口，按来源和手机号双重限速。项目本来就装了做这件事需要的组件，直接用。

### ✅ 04. 通行证从 30 天改成几小时，并且能收回

现在发出去的通行证 **30 天内一直有效**，而且一旦泄露，**作废不掉**——点了退出也只是本地忘掉，那张证在外面依然管用。

- **怎么做**：有效期缩短到几小时，配一张"续期凭条"让正常用户无感；同时给每张证编号，退出或出事时能挂失。

### ✅ 05. 把接口说明书从公网撤下来

项目自带一个自动生成的"接口目录"页面，现在**对所有人公开**，服务器配置里也明确把它放行到了公网。等于把整栋楼的房间号和用途贴在大门口。

- **怎么做**：正式环境关掉它，或者加上登录才能看。开发环境保留不影响。

---

## 二级 · 重要（5 项）— 完成 4 项，剩 06

> 判断标准：不会立刻出事，但决定这个项目**还能不能顺利做下去**——好不好玩、改得快不快、出了问题能不能退回去。

### 06. "必讲要点"改成程序清单，没讲完不许进下一阶段

作者自己记录了三个还没解决的毛病：**AI 角色该说的话会漏说、表现忽好忽坏、老是重复打电话**。我看下来，三个毛病是同一个原因——现在是"请 AI 自觉记得"，而不是程序强制。这是目前最影响好不好玩的一件事。

它其实违背了项目自己定的核心原则：*规则引擎说了算，AI 只负责把话说好听*。搜证、投票、证据谁能看，这些都已经严格由程序管住了，做得很好；唯独"该说的话"漏在了外面。

- **怎么做**：程序为每个角色每一轮列一张"必须说到的要点"清单，AI 说了就勾掉，没勾完就不许推进阶段。同一个动作重复触发时，由程序直接拦住，不靠 AI 的记性。
- **为什么**：比继续调 AI 话术更省钱也更可控。每轮调话术都要花真钱试，结果还不稳定；程序管住的部分则是一次做对、永远对。

### ✅ 07. 给五个并存的旧版本定一张退役表

作者每做一版新玩法都会留着旧版本"以防万一"。现在**同一套玩法有五个版本在系统里同时活着**。后果是：改任何一个小地方，都得同时考虑五种旧情况会不会坏。这是目前迭代变慢的主要原因，也是第 06 条改起来费劲的原因。

- **怎么做**：先从用得最少的两个老版本开始退役。立一条规矩——以后每上一个新版本，必须同时写明哪个旧版本什么时候下线。否则版本只增不减，会一直吃掉开发速度。

> **执行记录（07）**：清点后发现与原判断不同——真正的阻塞不在最旧的两个版本，
> 而在最新的 1.3/1.4：它们只有游玩侧实现，没有内容产出路径，实际承担产出的仍是 1.2。
> 已建立契约登记表（`backend/src/schemas/package_contract_registry.py`）把编译支持、
> 游玩支持、后继版本、保留理由和退役下一步逐条写明，并加测试强制新增版本必须登记。
> 本次未删除任何版本实现：删代码前需先依据真实数据确认存量清零。


### ✅ 08. 建立版本历史（已提前完成）

这个项目目录**还不是一个版本库**——改坏了退不回去，也看不出谁在什么时候改了什么。后面所有改动都需要这张安全网，所以越早越好。

- **怎么做**：在目录里初始化版本库并提交一次当前状态，作为基线。之后每完成一条改动提交一次。

### ✅ 09. 补一条自动检查：免登录名单必须对得上真实入口

系统里有一张"这些入口不用登录也能访问"的名单。我发现名单上有一条写着"语音合成入口免登录"，**但那个入口目前根本不存在**。哪天有人把它加回来，就等于凭空开了一个免费给陌生人烧钱的口子，而且没人会注意到。

- **怎么做**：加一条自动检查——名单上的每一条，都必须对应一个真实存在的入口，对不上就让检查不通过。这类隐患会自动暴露，不用靠人记得。

### ✅ 10. 把早就不用的"全 AI 模拟器"整体下架

项目里还留着一整套早期的、全部角色都由 AI 演的老游戏系统，大约 **6900 行**。它现在只是被"仅管理员可见"挡着，但十几个入口依然照常开着，每次升级、检查、打包都得带着它跑一遍。

- **怎么做**：改成打包时就不带进去，而不是靠运行时挡一下。少带十几个入口，等于少十几个可能出问题的地方。

---

## 三级 · 改进（5 项）

> 判断标准：现在不做也没事，但拖得越久越麻烦。有余力就顺手做掉。

### 11. 清掉三千多条过时写法

全项目有三千多条"这种写法以后会被淘汰"的提醒。现在还能跑，但下次大版本升级会**一次性全部爆掉**，到时候是被迫在压力下改。

- **怎么做**：趁现在分批换掉，每次换一类，别等被迫升级那天。

### 12. 把"演得好不好"变成能自动打分的东西

现在判断 AI 演得好不好，靠人一局一局试玩，慢、贵、而且说不清到底改好了没有。

- **怎么做**：固定几局标准剧本当考卷，每次改动后自动跑一遍打分——要点漏了几条、重复了几次、有没有说出不该知道的信息。这样"这次改动是变好还是变坏"就有客观答案了。
- **提示**：做完第 06 条之后再做这条，效果最好——先有清单，才有分可打。

### 13. 把测试用的脚手架搬出正式程序

大约 **2900 行**测试和试跑用的代码，被放在了正式程序的文件夹里，会跟着一起打包上线。

- **怎么做**：搬到专门的测试目录，打包时排除掉。正式程序立刻瘦一圈。

### 14. 开机别再干等外部服务，拆开最大的那个文件

程序启动时会同步去连文件存储服务，连不上还要重试 5 次，我实测**卡住约 6 秒**才继续。另外核心玩法全挤在一个 **1827 行**的文件里，改动风险高。

- **怎么做**：存储改成用到时再连并设超时上限，本地开发默认用本机存储；大文件按职责拆成几个。

### 15. 把说明书理一理

项目有 **152 篇**文档，光开发记录就 77 篇，首页说明也长成了流水账。新人很难判断"现在到底是什么状态"。

- **怎么做**：拆成两份——一页"当前状态"，和一份按时间排的变更记录。其余归档。

---

## 这个排序是怎么定的

**一级看"会不会出事"。** 这五条的共同点是：不做，就会有真实损失发生，而且不需要对方多厉害。其中 01 和 02 是最紧的，因为它们现在都处于"默认就是不安全"的状态——不是有人疏忽了，而是照着安装说明装完就是这样。

**二级看"还能不能做下去"。** 06 排在二级第一，因为它直接决定游戏好不好玩；07 紧随其后，因为不清掉并存的旧版本，06 会改得非常痛苦。08 虽然简单，但它是后面所有改动的安全网，所以提到了二级。

**三级看"拖着会不会更贵"。** 这五条现在都不痛，但拖久了成本会涨——尤其是 11，等到被迫升级那天，三千多条要一起改。

---

## 一级做完没有，你自己就能验

下面六条不需要看代码。全部满足，说明一级真的完成了，可以考虑对外开放。

- [x] 用别人的手机号 + `123456`，登不进去。 —— 对应 01：假验证码已关掉
- [x] 收到的验证码只出现在短信里，网页上看不到。 —— 对应 01：验证码不再泄露给调用方
- [x] 故意删掉配置里的签名钥匙，程序起不来，并告诉你缺了什么。 —— 对应 02：开机自查生效
- [x] 连续快速点十几次"获取验证码"，会被提示稍后再试。 —— 对应 03：防刷生效
- [x] 登录后放几个小时不动，回来需要重新验证。 —— 对应 04：通行证有效期收短了
- [x] 没登录的情况下打开网站的接口说明书页面，打不开。 —— 对应 05：接口目录已从公网撤下

以上六条已在本机端到端跑通验证。**注意：第 01 条只完成了「不再泄露、不再是固定码、
生产环境拒绝启动」这一半；真实短信供应商仍需你自己开通账号后接入。**

---

*本方案基于 2026-09-12 在本机的一次完整跑通：启动了整个后端、清点了全部 132 个接口、跑完前后端全部自动检查、完成一次正式打包，并对上面提到的安全问题做了实际验证。全程未调用任何付费模型，未改动项目中的任何代码文件。*
